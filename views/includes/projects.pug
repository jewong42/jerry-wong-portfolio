div(id="projects" class="container")
  h1(class="display-4 text-capitalize text-center") Selective projects
  hr(class="mb-5")
  div(class="container w-75")
    div(class="row justify-content-around")
      each project in projects
        div(class="col-auto mb-4")
          div(class="card" style="width: 15rem; height: 27rem")
            a(href="#")
              img(class="card-img-top border-bottom" src=project.thumbnail)
            div(class="card-body d-flex flex-column")
              h5(class="card-title") #{project.name}
              small(class="card-text") #{project.description_short}
              button(class="btn btn-primary btn-sm text-capitalize mt-auto" type="button" data-toggle="modal" data-target="#project_modal" data-name=project.name data-role=project.role data-tech=project.tech data-language=project.language data-descrption=project.description_long) More info